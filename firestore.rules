rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write only if user is signed in (for future use)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
